{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: FastAPI (root main)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
            ],
            "cwd": "${workspaceFolder}/backend",
            "jinja": true
        }
    ]
}